# Uni-Poly 项目核心架构文档

## 一、项目概述

### 1.1 项目目标
Uni-Poly 是一个**多模态聚合物性质预测框架**，通过整合多种分子表示方法来构建统一的聚合物表示，用于预测聚合物的各种物理化学性质。

### 1.2 核心创新点
- **多模态融合**：整合5种不同的聚合物表示方式（SMILES序列、2D图、3D几何、分子指纹、文本描述）
- **领域知识整合**：通过文本描述引入领域专业知识
- **统一表示学习**：使用注意力机制融合多模态信息，学习统一的聚合物表示

### 1.3 支持的性质预测任务
- `tg`: 玻璃化转变温度 (Glass Transition Temperature)
- `er`: 弹性模量 (Elastic Modulus)
- `de`: 介电常数 (Dielectric Constant)
- `td`: 热分解温度 (Thermal Decomposition Temperature)
- `tm`: 熔点 (Melting Point)
- `iv`: 固有粘度 (Intrinsic Viscosity)
- `bc`: 生物相容性 (Biocompatibility)

---

## 二、模型架构

### 2.1 整体架构图

```
输入数据 (聚合物SMILES)
    │
    ├─→ SMILES编码器 (RoBERTa) ──┐
    ├─→ 文本编码器 (T5) ─────────┤
    ├─→ 图编码器 (GIN) ───────────┤
    ├─→ 指纹编码器 (MLP) ─────────┤  → 投影层 → 统一嵌入空间
    └─→ 几何编码器 (SchNet) ──────┘
                                    │
                                    ↓
                            多模态融合模块
                            (多头注意力 + FFN)
                                    │
                                    ↓
                            注意力池化层
                                    │
                                    ↓
                            最终MLP预测层
                                    │
                                    ↓
                              性质预测值
```

### 2.2 核心组件说明

#### 2.2.1 多模态编码器 (`UniEncoderAttention`)

**位置**: `src/modules/uni_encoder.py`

**功能**: 整个模型的主控制器，负责协调各个模态编码器并进行融合。

**关键组件**:
1. **编码器模块字典** (`self.encoders`): 为每种模态创建对应的编码器
2. **融合模块** (`self.fusion_module`): 使用Transformer架构融合多模态特征
3. **预测MLP** (`self.mlp`): 将融合后的特征映射到性质预测值

**前向传播流程**:
```python
# 1. 对每种模态分别编码
embeddings = []
for modality in self.modality_list:
    embeddings.append(self.encoders[modality](data))
embeddings = torch.stack(embeddings, dim=1)  # [batch, num_modalities, joint_dim]

# 2. 多模态融合
fused_output, modality_attention = self.fusion_module(embeddings)

# 3. 性质预测
output = self.mlp(fused_output)
```

#### 2.2.2 单模态编码器 (`EncoderModule`)

**功能**: 将不同模态的输入编码到统一的嵌入空间。

**支持的模态**:

1. **SMILES模态** (`smiles`)
   - **编码器**: RoBERTa (预训练模型: `seyonec/PubChem10M_SMILES_BPE_450k`)
   - **输入**: SMILES字符串的tokenized序列
   - **输出维度**: 768 → 256 (通过投影层)

2. **文本模态** (`text`)
   - **编码器**: T5 Encoder (预训练模型: `GT4SD/multitask-text-and-chemistry-t5-base-augm`)
   - **输入**: 聚合物描述文本的tokenized序列
   - **输出维度**: 768 → 256

3. **图模态** (`graph`)
   - **编码器**: GIN (Graph Isomorphism Network)
   - **预训练模型**: `./pretrained_models/Mole-BERT.pth`
   - **输入**: 2D分子图 (节点特征、边索引、边属性)
   - **架构**: 5层GIN + Mean Pooling
   - **输出维度**: 300 → 256

4. **指纹模态** (`fp`)
   - **编码器**: 简单MLP投影层
   - **输入**: Morgan指纹 (1024维)
   - **输出维度**: 1024 → 256

5. **几何模态** (`geom`)
   - **编码器**: SchNet (基于3D坐标的图神经网络)
   - **预训练模型**: `./pretrained_models/schnet_qm9_heat_capacity_model.pth`
   - **输入**: 3D原子坐标和原子序数
   - **输出维度**: 128 → 256

**投影层结构**:
```python
nn.Sequential(
    nn.Linear(input_dim, joint_embedding_dim),  # 统一到256维
    nn.BatchNorm1d(joint_embedding_dim),
    nn.ReLU()
)
```

#### 2.2.3 融合模块 (`FusionModule`)

**功能**: 使用Transformer架构融合多模态特征。

**架构**:
1. **多头自注意力层** (`MultiheadAttention`)
   - 头数: 8
   - 嵌入维度: 256
   - 作用: 让不同模态之间相互关注，学习模态间的关联

2. **前馈网络** (`FeedForward`)
   - 输入维度: 256
   - 隐藏维度: 512 (默认)
   - 激活函数: ReLU

3. **层归一化** (`LayerNorm`)
   - 两次Add & Norm操作，遵循Transformer标准结构

4. **注意力池化** (`AttentionPooling`)
   - 学习每个模态的重要性权重
   - 输出: 加权平均后的融合特征

**前向传播**:
```python
# 1. 多头自注意力
attn_output = multihead_attn(embeddings, embeddings, embeddings)
fused_embedding = layer_norm1(embeddings + attn_output)

# 2. 前馈网络
ff_output = feed_forward(fused_embedding)
fused_embedding = layer_norm2(fused_embedding + ff_output)

# 3. 注意力池化
output, attention_weights = attention_pooling(fused_embedding)
```

---

## 三、数据处理流程

### 3.1 数据集类 (`UniDataset`)

**位置**: `src/dataset/dataset.py`

**功能**: 将原始CSV数据转换为模型可用的多模态数据格式。

**处理流程**:

1. **数据加载**
   - 从 `data/raw/{dataset}.csv` 读取数据
   - CSV格式: `SMILES, property_value`

2. **SMILES处理**
   - 使用RDKit解析SMILES字符串
   - 处理特殊原子（如`*`星号原子）
   - Tokenize SMILES序列

3. **文本处理**
   - 从 `data/smiles_text_dict.json` 加载文本描述
   - Tokenize文本描述

4. **图数据生成** (`graph_data.py`)
   - 将分子转换为图结构
   - 节点特征: 原子类型、手性标签
   - 边特征: 键类型、键方向
   - 使用 `mol_to_graph_data_obj_simple()` 函数

5. **3D坐标生成** (`geom_data.py`)
   - 使用RDKit生成3D分子构象
   - 如果原子数>400，使用2D坐标
   - 使用 `mol2coords()` 函数

6. **指纹生成**
   - 使用Morgan指纹生成器 (半径=2, 大小=1024)

7. **数据缓存**
   - 处理后的数据保存到 `data/processed/{dataset}.pt`
   - 下次直接加载，避免重复处理

**数据对象结构**:
```python
Data(
    x: 节点特征 [num_nodes, 2],
    edge_index: 边索引 [2, num_edges],
    edge_attr: 边属性 [num_edges, 2],
    batch: 批次索引 [num_nodes],
    y: 性质标签 [1],
    smiles: SMILES字符串,
    input_ids_smiles: SMILES token IDs [1, max_len],
    attention_mask_smiles: SMILES attention mask [1, max_len],
    text: 文本描述,
    input_ids_text: 文本 token IDs [1, max_len],
    attention_mask_text: 文本 attention mask [1, max_len],
    fp: 分子指纹 [1, 1024],
    pos3d: 3D坐标 [num_atoms, 3],
    z: 原子序数 [num_atoms],
    batch3d: 3D批次索引 [num_atoms]
)
```

### 3.2 数据加载器 (`custom_collate`)

**位置**: `src/dataset/dataloader.py`

**功能**: 将多个数据对象批量化，处理不同大小的图结构。

---

## 四、训练流程

### 4.1 训练脚本 (`train.py`)

**位置**: `scripts/train.py`

**训练流程**:

1. **参数解析**
   - 任务列表、模态列表、训练超参数等

2. **数据准备**
   - 加载数据集
   - 数据标准化（某些任务使用log10变换）
   - 划分训练/验证/测试集 (8:1:1)

3. **模型初始化**
   - 创建 `UniEncoderAttention` 模型
   - 加载预训练权重（可选）
   - 移动到GPU

4. **训练循环**
   - 损失函数: MSE Loss
   - 优化器: Adam (lr=1e-4)
   - 学习率调度: CosineAnnealingLR
   - 早停机制: 基于验证集R²

5. **评估**
   - 在测试集上评估
   - 计算R²、MAE、RMSE指标
   - 保存最佳模型

**关键训练函数** (`utils.py`):

- `train_epoch()`: 单个epoch的训练
- `evaluate()`: 模型评估
- `test_model()`: 测试集评估（反标准化后）
- `train_and_evaluate()`: 完整的训练和评估流程

### 4.2 预训练流程 (`pretrain.py`)

**位置**: `scripts/pretrain.py`

**功能**: 使用对比学习预训练多模态编码器。

**预训练目标**:
- 让同一聚合物的不同模态表示在嵌入空间中靠近
- 让不同聚合物的表示在嵌入空间中远离

**损失函数**: 对比损失 (`compute_contrastive_loss`)
- 计算不同模态之间的相似度
- 使用交叉熵损失，正样本为同一聚合物的不同模态

---

## 五、关键代码文件说明

### 5.1 核心模块文件

| 文件路径 | 主要类/函数 | 功能说明 |
|---------|------------|---------|
| `src/modules/uni_encoder.py` | `UniEncoderAttention` | 主模型类，多模态融合 |
| `src/modules/uni_encoder.py` | `EncoderModule` | 单模态编码器 |
| `src/modules/uni_encoder.py` | `FusionModule` | 多模态融合模块 |
| `src/modules/graph.py` | `GNN_graphpred` | 图神经网络编码器 |
| `src/modules/geom.py` | `SchNetEncoder` | 3D几何编码器 |

### 5.2 数据处理文件

| 文件路径 | 主要功能 |
|---------|---------|
| `src/dataset/dataset.py` | 数据集类，数据预处理 |
| `src/dataset/graph_data.py` | 分子图数据转换 |
| `src/dataset/geom_data.py` | 3D坐标生成 |
| `src/dataset/dataloader.py` | 批量化处理 |

### 5.3 工具函数文件

| 文件路径 | 主要功能 |
|---------|---------|
| `src/utils.py` | 训练、评估、数据加载工具函数 |

### 5.4 训练脚本

| 文件路径 | 功能 |
|---------|------|
| `scripts/train.py` | 主训练脚本 |
| `scripts/pretrain.py` | 预训练脚本 |
| `scripts/run_train.sh` | 训练启动脚本 |
| `scripts/run_pretrain.sh` | 预训练启动脚本 |

---

## 六、模型输入输出

### 6.1 输入格式

**单个样本输入**:
- SMILES字符串: `"CCO"` (乙醇)
- 对应的文本描述: `"poly(1-phenylethylene)"` (从JSON文件加载)
- 性质标签: 浮点数 (如玻璃化转变温度)

### 6.2 输出格式

**模型输出**:
- 预测值: `[batch_size, 1]` 张量
- 多模态嵌入: `[batch_size, num_modalities, joint_embedding_dim]` (可选)
- 注意力权重: `[batch_size, num_modalities]` (用于可视化)

### 6.3 评估指标

- **R² (决定系数)**: 衡量预测值与真实值的相关性
- **MAE (平均绝对误差)**: 预测误差的平均值
- **RMSE (均方根误差)**: 预测误差的平方根

---

## 七、预训练模型

### 7.1 需要的预训练权重

| 模态 | 模型名称 | 路径/来源 |
|------|---------|----------|
| SMILES | PubChem10M_SMILES_BPE_450k | HuggingFace: `seyonec/PubChem10M_SMILES_BPE_450k` |
| Text | multitask-text-and-chemistry-t5-base-augm | HuggingFace: `GT4SD/multitask-text-and-chemistry-t5-base-augm` |
| Graph | Mole-BERT | `./pretrained_models/Mole-BERT.pth` |
| Geometry | SchNet QM9 | `./pretrained_models/schnet_qm9_heat_capacity_model.pth` |

### 7.2 模型权重加载

- SMILES和Text模型: 自动从HuggingFace下载
- Graph和Geometry模型: 需要手动下载到指定路径

---

## 八、使用示例

### 8.1 训练模型

```bash
# 使用默认参数训练
bash scripts/run_train.sh

# 自定义参数训练
python scripts/train.py \
    --tasks tg er de \
    --modalities smiles text graph fp geom \
    --epochs 100 \
    --batch_size 32 \
    --freeze_encoder
```

### 8.2 预训练模型

```bash
# 预训练多模态编码器
bash scripts/run_pretrain.sh

# 自定义预训练参数
python scripts/pretrain.py \
    --modalities smiles text graph fp geom \
    --epochs 10 \
    --batch_size 32 \
    --lr 1e-3
```

### 8.3 生成文本描述

```bash
# 使用Jupyter Notebook生成聚合物描述
cd caption_generation
jupyter notebook Poly-caption.ipynb
```

---

## 九、项目目录结构

```
Uni-Poly/
├── src/                          # 源代码目录
│   ├── modules/                  # 模型模块
│   │   ├── uni_encoder.py       # 多模态编码器（核心）
│   │   ├── graph.py             # 图神经网络
│   │   └── geom.py              # 几何编码器
│   ├── dataset/                  # 数据处理
│   │   ├── dataset.py           # 数据集类
│   │   ├── graph_data.py        # 图数据转换
│   │   ├── geom_data.py         # 3D坐标生成
│   │   └── dataloader.py        # 数据加载器
│   └── utils.py                  # 工具函数
├── scripts/                       # 训练脚本
│   ├── train.py                 # 主训练脚本
│   ├── pretrain.py              # 预训练脚本
│   └── *.sh                     # Shell启动脚本
├── data/                         # 数据目录
│   ├── raw/                     # 原始CSV数据
│   ├── processed/               # 处理后的数据
│   └── smiles_text_dict.json    # SMILES到文本的映射
├── pretrained_models/            # 预训练模型权重
├── caption_generation/           # 文本描述生成工具
└── requirements.txt              # 依赖包列表
```

---

## 十、关键技术细节

### 10.1 多模态融合策略

- **统一嵌入空间**: 所有模态投影到256维统一空间
- **注意力机制**: 使用多头自注意力学习模态间关系
- **注意力池化**: 学习每个模态的重要性权重

### 10.2 数据标准化

- 对于某些任务（`er`, `iv`），使用log10变换
- 所有任务都使用StandardScaler进行标准化
- 测试时进行反标准化得到原始尺度

### 10.3 训练技巧

- **早停机制**: 基于验证集R²，默认patience=10
- **学习率调度**: CosineAnnealingLR
- **批次大小**: 默认32
- **优化器**: Adam，学习率1e-4

### 10.4 模型可解释性

- 输出注意力权重，可视化不同模态的重要性
- 可以分析哪些模态对特定性质预测更重要

---

## 十一、扩展方向

1. **添加新模态**: 在 `EncoderModule` 中添加新的模态处理逻辑
2. **修改融合策略**: 修改 `FusionModule` 的架构
3. **新任务支持**: 添加新的性质预测任务
4. **模型优化**: 尝试不同的预训练模型或架构变体

---

## 十二、常见问题

### Q1: 如何处理缺失的模态数据？
A: 当前实现要求所有模态都存在。如需支持缺失模态，需要修改 `EncoderModule` 的forward方法。

### Q2: 如何添加新的预训练模型？
A: 在 `EncoderModule._initialize_encoder()` 中添加新的模态分支，并实现对应的forward逻辑。

### Q3: 如何可视化注意力权重？
A: 模型训练后会保存 `attention_visual_weights`，可以使用matplotlib等工具可视化。

### Q4: 预训练是否必需？
A: 不是必需的，但预训练可以提高模型性能，特别是在数据量较小的情况下。

---

## 十三、参考文献

- **GIN**: Graph Isomorphism Network (Xu et al., 2019)
- **SchNet**: Schütt et al., 2018
- **RoBERTa**: Liu et al., 2019
- **T5**: Raffel et al., 2020

---

**文档版本**: v1.0  
**最后更新**: 2024  
**维护者**: Uni-Poly开发团队

