# 数据处理流程详解

## 📋 目录

1. [Graph数据中的节点特征和边特征](#1-graph数据中的节点特征和边特征)
2. [Morgan指纹生成器中的半径含义](#2-morgan指纹生成器中的半径含义)
3. [其他分子指纹类型](#3-其他分子指纹类型)

---

## 1. Graph数据中的节点特征和边特征

### 1.1 什么是分子图？

在化学信息学中，**分子图（Molecular Graph）**是将分子结构表示为图数据结构：
- **节点（Node/Vertex）** = 原子（Atom）
- **边（Edge）** = 化学键（Bond）

### 1.2 节点特征（Node Features）

**位置**: `src/dataset/graph_data.py:45-53`

**代码实现**:
```45:53:src/dataset/graph_data.py
    # atoms
    num_atom_features = 2   # atom type,  chirality tag
    atom_features_list = []
    for atom in mol.GetAtoms():
        atom_feature = [allowable_features['possible_atomic_num_list'].index(
            atom.GetAtomicNum())] + [allowable_features[
            'possible_chirality_list'].index(atom.GetChiralTag())]
        atom_features_list.append(atom_feature)
    x = torch.tensor(np.array(atom_features_list), dtype=torch.long)
```

**节点特征包含2个维度**:

#### 维度1: 原子类型（Atomic Number）
- **含义**: 原子的原子序数，表示原子的种类
- **取值范围**: 1-118（氢到Oganesson）
- **示例**:
  - 1 = 氢（H）
  - 6 = 碳（C）
  - 7 = 氮（N）
  - 8 = 氧（O）
- **代码**: `atom.GetAtomicNum()`

#### 维度2: 手性标签（Chirality Tag）
- **含义**: 原子的手性信息，表示原子的立体化学构型
- **可能的值**:
  ```python
  'possible_chirality_list' : [
      Chem.rdchem.ChiralType.CHI_UNSPECIFIED,      # 0: 未指定手性
      Chem.rdchem.ChiralType.CHI_TETRAHEDRAL_CW,    # 1: 顺时针四面体手性
      Chem.rdchem.ChiralType.CHI_TETRAHEDRAL_CCW,   # 2: 逆时针四面体手性
      Chem.rdchem.ChiralType.CHI_OTHER               # 3: 其他手性类型
  ]
  ```
- **代码**: `atom.GetChiralTag()`

**节点特征矩阵 `x` 的形状**:
- `[num_atoms, 2]` - 每个原子有2个特征
- 例如：一个包含10个原子的分子，`x.shape = [10, 2]`

**实际例子**:
```python
# 假设一个简单的分子：乙醇 (CCO)
# 原子: C(0), C(1), O(2), H(3), H(4), H(5), H(6), H(7), H(8)
# x = [
#   [6, 0],  # C(0): 原子序数6, 无手性
#   [6, 0],  # C(1): 原子序数6, 无手性
#   [8, 0],  # O(2): 原子序数8, 无手性
#   [1, 0],  # H(3): 原子序数1, 无手性
#   ...
# ]
```

---

### 1.3 边特征（Edge Features）

**位置**: `src/dataset/graph_data.py:55-77`

**代码实现**:
```55:77:src/dataset/graph_data.py
    # bonds
    num_bond_features = 2   # bond type, bond direction
    if len(mol.GetBonds()) > 0: # mol has bonds
        edges_list = []
        edge_features_list = []
        for bond in mol.GetBonds():
            i = bond.GetBeginAtomIdx()
            j = bond.GetEndAtomIdx()
            edge_feature = [allowable_features['possible_bonds'].index(
                bond.GetBondType())] + [allowable_features[
                                            'possible_bond_dirs'].index(
                bond.GetBondDir())]
            edges_list.append((i, j))
            edge_features_list.append(edge_feature)
            edges_list.append((j, i))
            edge_features_list.append(edge_feature)

        # data.edge_index: Graph connectivity in COO format with shape [2, num_edges]
        edge_index = torch.tensor(np.array(edges_list).T, dtype=torch.long)

        # data.edge_attr: Edge feature matrix with shape [num_edges, num_edge_features]
        edge_attr = torch.tensor(np.array(edge_features_list),
                                 dtype=torch.long)
    else:   # mol has no bonds
        edge_index = torch.empty((2, 0), dtype=torch.long)
        edge_attr = torch.empty((0, num_bond_features), dtype=torch.long)
```

**边特征包含2个维度**:

#### 维度1: 键类型（Bond Type）
- **含义**: 化学键的类型，表示原子之间的连接强度
- **可能的值**:
  ```python
  'possible_bonds' : [
      Chem.rdchem.BondType.SINGLE,    # 0: 单键
      Chem.rdchem.BondType.DOUBLE,    # 1: 双键
      Chem.rdchem.BondType.TRIPLE,    # 2: 三键
      Chem.rdchem.BondType.AROMATIC   # 3: 芳香键
  ]
  ```
- **代码**: `bond.GetBondType()`

#### 维度2: 键方向（Bond Direction）
- **含义**: 化学键的方向信息，主要用于双键的立体化学
- **可能的值**:
  ```python
  'possible_bond_dirs' : [
      Chem.rdchem.BondDir.NONE,         # 0: 无方向
      Chem.rdchem.BondDir.ENDUPRIGHT,    # 1: 向上
      Chem.rdchem.BondDir.ENDDOWNRIGHT   # 2: 向下
  ]
  ```
- **代码**: `bond.GetBondDir()`

**边索引矩阵 `edge_index` 的形状**:
- `[2, num_edges]` - COO格式（Coordinate Format）
- 第一行：源节点索引
- 第二行：目标节点索引
- **注意**: 每条边会存储两次 `(i, j)` 和 `(j, i)`，因为图是无向图

**边特征矩阵 `edge_attr` 的形状**:
- `[num_edges, 2]` - 每条边有2个特征
- 与 `edge_index` 对应，每条边都有对应的特征

**实际例子**:
```python
# 假设一个简单的分子：乙烯 (C=C)
# 原子: C(0), C(1), H(2), H(3), H(4), H(5)
# 
# edge_index = [
#   [0, 1, 0, 2, 0, 3, 1, 4, 1, 5, 1, 0, 2, 0, 3, 0, 4, 1, 5, 1],  # 源节点
#   [1, 0, 2, 0, 3, 0, 4, 1, 5, 1, 0, 1, 0, 2, 0, 3, 1, 4, 1, 5]   # 目标节点
# ]
# 
# edge_attr = [
#   [1, 0],  # C(0)-C(1): 双键, 无方向
#   [1, 0],  # C(1)-C(0): 双键, 无方向（反向）
#   [0, 0],  # C(0)-H(2): 单键, 无方向
#   [0, 0],  # H(2)-C(0): 单键, 无方向（反向）
#   ...
# ]
```

---

### 1.4 为什么需要节点特征和边特征？

**节点特征的作用**:
- 告诉GNN每个原子的**类型**（是碳、氧还是氢？）
- 告诉GNN每个原子的**立体化学信息**（手性构型）

**边特征的作用**:
- 告诉GNN原子之间的**连接强度**（单键、双键还是三键？）
- 告诉GNN化学键的**立体化学信息**（双键的方向）

**在GNN中的作用**:
- GNN通过**消息传递机制**，让每个节点聚合邻居节点的信息
- 节点特征和边特征帮助GNN理解分子的**结构**和**化学性质**
- 例如：双键（边特征）连接的碳原子（节点特征）可能参与化学反应

---

## 2. Morgan指纹生成器中的半径含义

### 2.1 什么是Morgan指纹？

**Morgan指纹**（也称为**Circular Fingerprint**或**ECFP**）是一种将分子结构编码为固定长度向量的方法。

**位置**: `src/dataset/dataset.py:81`

```81:81:src/dataset/dataset.py
        mfpgen = rdFingerprintGenerator.GetMorganGenerator(radius=2,fpSize=1024)
```

**参数说明**:
- `radius=2`: 半径参数
- `fpSize=1024`: 指纹向量的大小（1024维）

---

### 2.2 半径（Radius）的含义

**半径（Radius）**表示从每个原子开始，**向外扩展的邻居层数**。

#### 半径 = 0
- 只考虑**原子本身**的特征
- 例如：原子类型、原子度等

#### 半径 = 1
- 考虑**原子本身 + 直接连接的邻居原子**
- 例如：C-C键、C-O键等

#### 半径 = 2（本项目使用）
- 考虑**原子本身 + 直接邻居 + 邻居的邻居**
- 可以捕获**局部子结构**（如官能团）

#### 半径 = 3
- 考虑更远的邻居
- 可以捕获更大的子结构

---

### 2.3 半径=2的详细解释

**算法流程**（以某个碳原子为例）:

```
步骤1: 初始化
  从原子C开始

步骤2: 半径=0（第0层）
  只包含原子C本身
  特征: [C]

步骤3: 半径=1（第1层）
  包含C + 直接连接的邻居
  例如: C连接了3个H和1个C
  特征: [C, (C, H, H, H)]

步骤4: 半径=2（第2层）
  包含C + 第1层邻居 + 第2层邻居
  例如: C连接了C，而那个C又连接了O和H
  特征: [C, (C, H, H, H), (C连接的C的邻居: O, H)]
```

**可视化示例**:
```
        半径=2的覆盖范围（以中心C为例）

        H
         \
          C
         / \
        H   C - O
             \
              H

半径=0: 只考虑中心C
半径=1: 考虑中心C + 直接连接的H, H, C
半径=2: 考虑中心C + 第1层(H, H, C) + 第2层(另一个C连接的O, H)
```

---

### 2.4 为什么选择半径=2？

**优点**:
1. **捕获局部结构**: 可以识别常见的官能团（如-OH, -COOH, -NH2等）
2. **计算效率**: 半径=2已经能捕获大部分重要的局部化学环境
3. **平衡**: 在表达能力和计算复杂度之间取得平衡

**半径选择的影响**:
- **半径太小（0或1）**: 可能无法捕获重要的局部结构
- **半径太大（3或更大）**: 
  - 计算成本增加
  - 可能过度泛化，失去局部特征的区分度
  - 对于大分子，可能覆盖整个分子

---

### 2.5 fpSize=1024的含义

**fpSize**表示生成的指纹向量的**长度（维度）**。

**生成过程**:
1. 对每个原子，根据其半径=2的局部环境，生成一个**哈希值**
2. 将哈希值映射到1024维向量的某个位置
3. 如果某个子结构出现，对应位置设为1（或计数）
4. 最终得到一个**1024维的二进制向量**（或计数向量）

**示例**:
```python
# 假设生成的指纹向量（简化版，实际是1024维）
fp = [0, 1, 0, 1, 1, 0, 0, 1, ..., 0]  # 1024维
      ↑  ↑  ↑  ↑  ↑  ↑  ↑  ↑
      位置0 位置1 位置2 位置3 位置4 位置5 位置6 位置7

# 1表示该子结构在分子中存在
# 0表示该子结构在分子中不存在
```

**为什么是1024？**
- **2的幂次**: 1024 = 2^10，便于计算机处理
- **平衡**: 足够大以捕获丰富的子结构信息，又不会太大导致稀疏性过高
- **标准选择**: 在化学信息学中，1024是常用的指纹长度

---

### 2.6 Morgan指纹的优势

1. **固定长度**: 无论分子大小，都输出1024维向量
2. **局部结构敏感**: 半径参数可以控制捕获的局部结构范围
3. **快速计算**: 计算效率高，适合大规模数据集
4. **广泛应用**: 在化学信息学中广泛使用，如相似性搜索、虚拟筛选等

---

## 3. 其他分子指纹类型

### 3.1 概述

虽然本项目目前只使用了**Morgan指纹**，但RDKit提供了多种分子指纹类型，每种都有其特点和适用场景。了解这些指纹类型有助于：
- 理解不同指纹的优缺点
- 在需要时选择合适的指纹类型
- 扩展项目以支持多种指纹

### 3.2 RDKit中常见的指纹类型

#### 3.2.1 Morgan指纹（ECFP/FCFP）- 当前使用

**全称**: Extended-Connectivity Fingerprints / Functional-Class Fingerprints

**特点**:
- ✅ **圆形指纹**：基于原子周围的环境
- ✅ **可调半径**：通过radius参数控制局部结构范围
- ✅ **固定长度**：可自定义fpSize（常用1024, 2048）
- ✅ **最常用**：在化学信息学中应用最广泛

**代码示例**:
```python
from rdkit.Chem import rdFingerprintGenerator

# 当前项目使用的方式
mfpgen = rdFingerprintGenerator.GetMorganGenerator(radius=2, fpSize=1024)
fp = mfpgen.GetFingerprint(mol)

# 或者使用AllChem（旧API，但仍然常用）
from rdkit.Chem import AllChem
fp = AllChem.GetMorganFingerprintAsBitVect(mol, radius=2, nBits=1024)
```

**适用场景**:
- 分子相似性搜索
- 虚拟筛选
- QSAR/QSPR建模
- 分子表示学习（如本项目）

---

#### 3.2.2 RDKit指纹（拓扑指纹）

**全称**: RDKit Topological Fingerprint

**特点**:
- ✅ **基于路径**：考虑分子图中的所有路径
- ✅ **固定长度**：默认2048位
- ✅ **路径长度可调**：通过minPath和maxPath参数控制
- ✅ **计算快速**：比Morgan指纹稍快

**代码示例**:
```python
from rdkit.Chem import rdFingerprintGenerator

# 使用新API
rdkit_gen = rdFingerprintGenerator.GetRDKitFPGenerator(minPath=1, maxPath=7, fpSize=2048)
fp = rdkit_gen.GetFingerprint(mol)

# 或使用旧API
from rdkit.Chem import rdMolDescriptors
fp = rdMolDescriptors.GetHashedTopologicalTorsionFingerprint(mol, nBits=2048)
```

**参数说明**:
- `minPath`: 最短路径长度（默认1）
- `maxPath`: 最长路径长度（默认7）
- `fpSize`: 指纹向量大小（默认2048）

**适用场景**:
- 需要捕获长距离路径信息
- 拓扑结构分析
- 分子相似性比较

---

#### 3.2.3 MACCS指纹

**全称**: Molecular ACCess System Keys

**特点**:
- ✅ **固定结构**：167个预定义的子结构特征
- ✅ **固定长度**：167位（固定，不可调整）
- ✅ **可解释性强**：每个位对应一个具体的化学子结构
- ✅ **历史悠久**：最早开发的分子指纹之一

**代码示例**:
```python
from rdkit.Chem import MACCSkeys

# MACCS指纹固定为167位
fp = MACCSkeys.GenMACCSKeys(mol)
# fp.GetNumBits() = 167
```

**MACCS特征示例**:
- 位1: 是否存在芳香环
- 位2: 是否存在羧基(-COOH)
- 位3: 是否存在羟基(-OH)
- ...（共167个特征）

**适用场景**:
- 需要可解释性的应用
- 药效团分析
- 子结构搜索

---

#### 3.2.4 原子对指纹（Atom Pair）

**特点**:
- ✅ **基于原子对**：考虑所有原子对之间的距离
- ✅ **距离编码**：编码原子类型和它们之间的距离
- ✅ **可变长度**：取决于分子中原子对的数量
- ✅ **拓扑信息**：捕获原子间的拓扑距离

**代码示例**:
```python
from rdkit.Chem import rdMolDescriptors

# 生成原子对指纹（返回稀疏向量）
atom_pairs = rdMolDescriptors.GetAtomPairFingerprint(mol)

# 转换为固定长度的位向量
from rdkit import DataStructs
fp = rdMolDescriptors.GetHashedAtomPairFingerprintAsBitVect(mol, nBits=2048)
```

**适用场景**:
- 需要捕获原子间距离信息
- 3D结构分析
- 分子相似性比较

---

#### 3.2.5 拓扑扭转指纹（Topological Torsion）

**特点**:
- ✅ **基于扭转角**：考虑4个连续原子的序列
- ✅ **局部结构**：捕获局部扭转信息
- ✅ **可变长度**：取决于分子中的扭转数量
- ✅ **立体化学敏感**：可以区分立体异构体

**代码示例**:
```python
from rdkit.Chem import rdMolDescriptors

# 生成拓扑扭转指纹
torsions = rdMolDescriptors.GetTopologicalTorsionFingerprint(mol)

# 转换为固定长度的位向量
fp = rdMolDescriptors.GetHashedTopologicalTorsionFingerprintAsBitVect(mol, nBits=2048)
```

**适用场景**:
- 需要捕获扭转角信息
- 构象分析
- 立体化学区分

---

#### 3.2.6 药效团指纹（Pharmacophore）

**特点**:
- ✅ **基于药效团**：考虑功能基团的空间排列
- ✅ **3D信息**：需要3D坐标
- ✅ **药物设计**：专门用于药物发现
- ✅ **距离矩阵**：编码功能基团之间的距离

**代码示例**:
```python
from rdkit.Chem import rdMolDescriptors

# 需要先生成3D坐标
from rdkit.Chem import AllChem
mol_3d = Chem.Mol(mol)
AllChem.EmbedMolecule(mol_3d)

# 生成药效团指纹
fp = rdMolDescriptors.GetPharmacophoreFingerprint(mol_3d)
```

**适用场景**:
- 药物设计
- 受体-配体相互作用分析
- 3D相似性搜索

---

### 3.3 指纹类型对比

| 指纹类型 | 长度 | 基于 | 优势 | 劣势 | 适用场景 |
|---------|------|------|------|------|---------|
| **Morgan (ECFP)** | 可调 (1024/2048) | 原子环境 | 最常用，平衡性好 | 需要调参 | 通用，相似性搜索 |
| **RDKit** | 可调 (2048) | 路径 | 快速，路径信息 | 可能丢失局部信息 | 拓扑分析 |
| **MACCS** | 固定 (167) | 子结构 | 可解释性强 | 特征少，不够灵活 | 可解释性需求 |
| **Atom Pair** | 可调 | 原子对距离 | 距离信息 | 计算较慢 | 距离分析 |
| **Topological Torsion** | 可调 | 扭转角 | 立体化学敏感 | 计算较慢 | 构象分析 |
| **Pharmacophore** | 可变 | 3D药效团 | 药物设计专用 | 需要3D坐标 | 药物发现 |

---

### 3.4 如何在本项目中使用其他指纹类型

#### 方案1: 修改数据集类支持多种指纹

```python
# 在 src/dataset/dataset.py 中
class UniDataset(Dataset):
    def __init__(self, ..., fingerprint_type='morgan'):
        self.fingerprint_type = fingerprint_type
        # 根据类型初始化不同的指纹生成器
        if fingerprint_type == 'morgan':
            self.fpgen = rdFingerprintGenerator.GetMorganGenerator(radius=2, fpSize=1024)
        elif fingerprint_type == 'rdkit':
            self.fpgen = rdFingerprintGenerator.GetRDKitFPGenerator(minPath=1, maxPath=7, fpSize=2048)
        elif fingerprint_type == 'maccs':
            from rdkit.Chem import MACCSkeys
            self.fpgen = None  # MACCS使用不同的API
        # ... 其他类型
    
    def process(self):
        # ...
        if self.fingerprint_type == 'morgan':
            data.fp = torch.tensor(self.fpgen.GetFingerprint(mol), dtype=torch.float).unsqueeze(0)
        elif self.fingerprint_type == 'rdkit':
            data.fp = torch.tensor(self.fpgen.GetFingerprint(mol), dtype=torch.float).unsqueeze(0)
        elif self.fingerprint_type == 'maccs':
            from rdkit.Chem import MACCSkeys
            fp = MACCSkeys.GenMACCSKeys(mol)
            data.fp = torch.tensor(fp, dtype=torch.float).unsqueeze(0)
        # ...
```

#### 方案2: 支持多指纹融合

```python
# 生成多种指纹并拼接
def generate_multiple_fingerprints(mol):
    fingerprints = []
    
    # Morgan指纹
    mfpgen = rdFingerprintGenerator.GetMorganGenerator(radius=2, fpSize=1024)
    fp_morgan = mfpgen.GetFingerprint(mol)
    fingerprints.append(torch.tensor(fp_morgan, dtype=torch.float))
    
    # RDKit指纹
    rdkit_gen = rdFingerprintGenerator.GetRDKitFPGenerator(minPath=1, maxPath=7, fpSize=2048)
    fp_rdkit = rdkit_gen.GetFingerprint(mol)
    fingerprints.append(torch.tensor(fp_rdkit, dtype=torch.float))
    
    # MACCS指纹
    from rdkit.Chem import MACCSkeys
    fp_maccs = MACCSkeys.GenMACCSKeys(mol)
    fingerprints.append(torch.tensor(fp_maccs, dtype=torch.float))
    
    # 拼接所有指纹
    combined_fp = torch.cat(fingerprints, dim=0)
    return combined_fp
```

**注意**: 如果使用多指纹融合，需要相应调整模型中的投影层输入维度。

---

### 3.5 选择指纹类型的建议

**选择Morgan指纹（当前选择）的情况**:
- ✅ 需要平衡表达能力和计算效率
- ✅ 需要捕获局部结构（官能团）
- ✅ 通用分子表示学习任务
- ✅ 大规模数据集

**选择RDKit指纹的情况**:
- ✅ 需要捕获长距离路径信息
- ✅ 计算速度要求高
- ✅ 拓扑结构分析

**选择MACCS指纹的情况**:
- ✅ 需要可解释性
- ✅ 需要预定义的化学特征
- ✅ 药效团分析

**选择Atom Pair或Topological Torsion的情况**:
- ✅ 需要距离或扭转角信息
- ✅ 立体化学分析
- ✅ 构象研究

**选择Pharmacophore指纹的情况**:
- ✅ 药物设计任务
- ✅ 有3D坐标数据
- ✅ 受体-配体相互作用

---

## 📊 总结对比

| 特征类型 | 维度 | 含义 | 作用 |
|---------|------|------|------|
| **节点特征** | 2 | 原子类型 + 手性标签 | 描述每个原子的性质 |
| **边特征** | 2 | 键类型 + 键方向 | 描述原子之间的连接 |
| **Morgan指纹** | 1024 | 局部子结构的二进制编码 | 描述分子的整体结构特征 |

---

## 🔍 代码位置总结

| 功能 | 文件位置 | 关键代码行 |
|------|---------|-----------|
| 节点特征生成 | `src/dataset/graph_data.py` | 45-53 |
| 边特征生成 | `src/dataset/graph_data.py` | 55-77 |
| Morgan指纹生成 | `src/dataset/dataset.py` | 81, 119 |

---

## 📚 扩展阅读

### RDKit指纹相关文档
- [RDKit Fingerprint Documentation](https://www.rdkit.org/docs/GettingStartedInPython.html#molecular-fingerprints)
- [Morgan Fingerprint详解](https://www.rdkit.org/docs/GettingStartedInPython.html#morgan-fingerprints-circular-fingerprints)
- [MACCS Keys详解](https://www.rdkit.org/docs/GettingStartedInPython.html#maccs-keys)

### 相关论文
- **ECFP**: Rogers, D., & Hahn, M. (2010). Extended-connectivity fingerprints. *Journal of chemical information and modeling*, 50(5), 742-754.
- **MACCS**: Durant, J. L., et al. (2002). Reoptimization of MDL keys for use in drug discovery. *Journal of chemical information and computer sciences*, 42(6), 1273-1280.

---

**文档版本**: v1.0  
**最后更新**: 2024  
**维护者**: Uni-Poly开发团队

