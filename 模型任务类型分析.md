# Uni-Poly 模型任务类型分析

## 📊 分析结论

**当前实现：只能做回归任务**  
**架构潜力：可以扩展支持分类任务**

---

## 🔍 证据分析

### 1. 输出层结构（回归特征）

**位置**: `src/modules/uni_encoder.py:55-61`

```55:61:src/modules/uni_encoder.py
        # Final MLP
        self.mlp = nn.Sequential(
            nn.Linear(joint_embedding_dim, 128),
            nn.ReLU(),
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, 1)
        )
```

**分析**:
- ✅ 输出维度为 `1` - 典型的回归任务输出（单个连续值）
- ❌ 没有 `Softmax` 或 `Sigmoid` 激活函数
- ❌ 没有多类别输出（如 `num_classes` 维度）

**如果是分类任务，应该是**:
```python
# 二分类
self.mlp = nn.Sequential(
    ...
    nn.Linear(64, 1),
    nn.Sigmoid()  # 输出概率
)

# 多分类
self.mlp = nn.Sequential(
    ...
    nn.Linear(64, num_classes),
    nn.Softmax(dim=1)  # 输出类别概率分布
)
```

---

### 2. 损失函数（回归损失）

**位置**: `src/utils.py:114`

```114:114:src/utils.py
    criterion = nn.MSELoss()
```

**分析**:
- ✅ 使用 `MSELoss` (均方误差损失) - 回归任务的经典损失函数
- ❌ 没有 `CrossEntropyLoss` (多分类)
- ❌ 没有 `BCELoss` 或 `BCEWithLogitsLoss` (二分类)

**如果是分类任务，应该使用**:
```python
# 二分类
criterion = nn.BCEWithLogitsLoss()

# 多分类
criterion = nn.CrossEntropyLoss()
```

---

### 3. 评估指标（回归指标）

**位置**: `src/utils.py:106-108`

```106:108:src/utils.py
    test_r2 = r2_score(y_true_unscaled, y_pred_unscaled)
    test_mae = metrics.mean_absolute_error(y_true_unscaled, y_pred_unscaled)
    test_rmse = np.sqrt(metrics.mean_squared_error(y_true_unscaled, y_pred_unscaled))
```

**分析**:
- ✅ `R²` (决定系数) - 回归任务指标
- ✅ `MAE` (平均绝对误差) - 回归任务指标
- ✅ `RMSE` (均方根误差) - 回归任务指标
- ❌ 没有 `accuracy` (准确率)
- ❌ 没有 `f1_score` (F1分数)
- ❌ 没有 `precision`/`recall` (精确率/召回率)

**如果是分类任务，应该使用**:
```python
# 二分类/多分类
accuracy = accuracy_score(y_true, y_pred)
f1 = f1_score(y_true, y_pred)
precision = precision_score(y_true, y_pred)
recall = recall_score(y_true, y_pred)
```

---

### 4. 数据标签格式（连续值）

**位置**: `src/dataset/dataset.py:95`

```95:95:src/dataset/dataset.py
            data.y = torch.tensor([property], dtype=torch.float)
```

**分析**:
- ✅ 标签类型为 `torch.float` - 浮点数，适合回归任务
- ❌ 不是 `torch.long` (整数类别标签)
- ❌ 不是 `torch.int` (整数类别标签)

**如果是分类任务，应该是**:
```python
# 二分类：0或1
data.y = torch.tensor([class_label], dtype=torch.long)

# 多分类：0, 1, 2, ..., num_classes-1
data.y = torch.tensor([class_label], dtype=torch.long)
```

---

### 5. 数据预处理（回归预处理）

**位置**: `src/utils.py:12-27`

```12:27:src/utils.py
def scale_targets(dataset, task):
    scaler = StandardScaler()
    y_values = np.array([data.y.item() for data in dataset]).reshape(-1, 1)

    if task in ['er', 'iv']:
        y_values = np.log10(y_values)
        for data in dataset:
            data.y = torch.tensor(np.log10(data.y.item()), dtype=torch.float)

    scaler.fit(y_values)
    print("Scaling y values with mean:", scaler.mean_[0], "and std:", scaler.scale_[0])

    for data in dataset:
        data.y = torch.tensor(scaler.transform([[data.y.item()]]), dtype=torch.float)

    return scaler
```

**分析**:
- ✅ 使用 `StandardScaler` 标准化连续值 - 回归任务预处理
- ✅ 对某些任务使用 `log10` 变换 - 处理偏态分布的回归任务
- ❌ 没有类别编码（如 `LabelEncoder`）
- ❌ 没有类别平衡处理

**如果是分类任务，应该使用**:
```python
# 类别编码
from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()
y_encoded = le.fit_transform(y_labels)

# 类别平衡
from sklearn.utils.class_weight import compute_class_weight
class_weights = compute_class_weight('balanced', classes=np.unique(y), y=y)
```

---

### 6. 任务列表（连续值性质）

**位置**: `scripts/train.py:20`

```20:20:scripts/train.py
        default=['tg', 'er', 'de', 'td', 'tm', 'iv', 'bc'],
```

**分析**:
- `tg`: 玻璃化转变温度 - **连续值** ✅
- `er`: 弹性模量 - **连续值** ✅
- `de`: 介电常数 - **连续值** ✅
- `td`: 热分解温度 - **连续值** ✅
- `tm`: 熔点 - **连续值** ✅
- `iv`: 固有粘度 - **连续值** ✅
- `bc`: 生物相容性 - 虽然可以是分类任务，但**当前作为回归处理** ✅

---

### 7. GNN编码器配置

**位置**: `src/modules/uni_encoder.py:133-141`

```133:141:src/modules/uni_encoder.py
        elif modality == 'graph':
            encoder = GNN_graphpred(
                num_layer=5,
                emb_dim=300,
                num_tasks=1,
                JK="last",
                drop_ratio=0,
                gnn_type="gin",
                graph_pooling="mean"
            )
```

**分析**:
- ✅ `num_tasks=1` - 固定为单任务回归
- ⚠️ 虽然 `GNN_graphpred` 支持 `num_tasks` 参数（可用于多任务学习），但这里固定为1

**注意**: `graph.py:294` 中有注释提到 "binary classification"，但实际使用中并未实现分类功能。

---

## 🎯 总结

### 当前实现：**只能做回归任务**

| 组件 | 当前实现 | 回归特征 |
|------|---------|---------|
| 输出层 | `Linear(64, 1)` | ✅ 输出1个连续值 |
| 损失函数 | `MSELoss()` | ✅ 回归损失 |
| 评估指标 | R², MAE, RMSE | ✅ 回归指标 |
| 标签格式 | `torch.float` | ✅ 浮点数 |
| 数据预处理 | StandardScaler + log10 | ✅ 回归预处理 |
| 任务类型 | 连续值性质预测 | ✅ 回归任务 |

---

## 🔧 如何扩展支持分类任务

### 方案1: 修改模型架构支持任务类型参数

```python
class UniEncoderAttention(nn.Module):
    def __init__(
        self,
        joint_embedding_dim: int,
        task_type: str = 'regression',  # 'regression' or 'classification'
        num_classes: int = 2,  # 分类任务的类别数
        ...
    ):
        ...
        
        # 根据任务类型构建输出层
        if task_type == 'regression':
            self.mlp = nn.Sequential(
                nn.Linear(joint_embedding_dim, 128),
                nn.ReLU(),
                nn.Linear(128, 64),
                nn.ReLU(),
                nn.Linear(64, 1)  # 回归：输出1个值
            )
        elif task_type == 'classification':
            self.mlp = nn.Sequential(
                nn.Linear(joint_embedding_dim, 128),
                nn.ReLU(),
                nn.Linear(128, 64),
                nn.ReLU(),
                nn.Linear(64, num_classes)  # 分类：输出类别数
            )
            self.task_type = 'classification'
        else:
            raise ValueError(f"Unsupported task_type: {task_type}")
```

### 方案2: 修改损失函数和评估指标

```python
def train_and_evaluate(model, scaler, train_loader, val_loader, test_loader, 
                      device, num_epochs=100, patience=5, task_type='regression'):
    
    # 根据任务类型选择损失函数
    if task_type == 'regression':
        criterion = nn.MSELoss()
    elif task_type == 'classification':
        criterion = nn.CrossEntropyLoss()
    else:
        raise ValueError(f"Unsupported task_type: {task_type}")
    
    ...
    
    # 根据任务类型选择评估指标
    if task_type == 'regression':
        test_r2 = r2_score(y_true, y_pred)
        test_mae = metrics.mean_absolute_error(y_true, y_pred)
        test_rmse = np.sqrt(metrics.mean_squared_error(y_true, y_pred))
        return {'test_r2': test_r2, 'test_mae': test_mae, 'test_rmse': test_rmse}
    elif task_type == 'classification':
        test_accuracy = accuracy_score(y_true, y_pred)
        test_f1 = f1_score(y_true, y_pred, average='weighted')
        test_precision = precision_score(y_true, y_pred, average='weighted')
        test_recall = recall_score(y_true, y_pred, average='weighted')
        return {'test_accuracy': test_accuracy, 'test_f1': test_f1, 
                'test_precision': test_precision, 'test_recall': test_recall}
```

### 方案3: 修改数据预处理

```python
def prepare_targets(dataset, task, task_type='regression'):
    if task_type == 'regression':
        # 回归任务：标准化
        scaler = StandardScaler()
        y_values = np.array([data.y.item() for data in dataset]).reshape(-1, 1)
        
        if task in ['er', 'iv']:
            y_values = np.log10(y_values)
        
        scaler.fit(y_values)
        for data in dataset:
            data.y = torch.tensor(scaler.transform([[data.y.item()]]), dtype=torch.float)
        
        return scaler
    
    elif task_type == 'classification':
        # 分类任务：类别编码
        from sklearn.preprocessing import LabelEncoder
        le = LabelEncoder()
        y_labels = [data.y.item() for data in dataset]
        y_encoded = le.fit_transform(y_labels)
        
        for i, data in enumerate(dataset):
            data.y = torch.tensor(y_encoded[i], dtype=torch.long)
        
        return le
```

---

## 📝 结论

1. **当前实现确实只能做回归任务** - 所有代码组件都指向回归任务
2. **架构本身可以扩展** - 多模态编码和融合部分与任务类型无关
3. **需要修改的关键点**:
   - 输出层结构（添加分类头）
   - 损失函数（改为分类损失）
   - 评估指标（改为分类指标）
   - 数据预处理（改为类别编码）
   - 标签格式（改为整数类别）

4. **建议**: 如果要支持分类任务，最好通过参数化设计，让模型同时支持回归和分类，而不是完全重写。

---

**文档版本**: v1.0  
**分析日期**: 2024  
**分析者**: AI Assistant

